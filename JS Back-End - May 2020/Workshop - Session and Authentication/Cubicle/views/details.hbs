<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Cubicle</title>
  <link href="/css/site.css" type="text/css" rel="stylesheet">
  <link rel='shortcut icon' type='image/x-icon' href='/images/favicon.png' />
</head>

<body>
  <div class="container">
    <header>
      <ul>
        <li><img class="logo" src="/images/logo.png"></li>
        <li><a href="/">Browse</a></li>
        <li><a href="/create">Add a Cube</a></li>
        <li><a href="/create/accessory">Add Accessory</a></li>
        <li><a href="/about">About</a></li>
        {{#if user}}
        <li><a href="/logout">Logout</a></li>
        {{else}}
        <li><a href="/login">Login</a></li>
        <li><a href="/register">Register</a></li>
        {{/if}}
      </ul>
    </header>
    <main>
      <h1>{{viewModel.cube.name}}</h1>
      <img class="cube" src="{{viewModel.cube.imageUrl}}">
      <div class="details">
        <p><span>Description:</span>{{viewModel.cube.description}}</p>
        <p><span>Difficulty level:</span>{{viewModel.cube.difficultyLevel}}</p>
        <a class="btn" href="/">Back</a>
      </div>
      <h2>Accessories</h2>
      {{#if viewModel.cube.HasAccessories}}
      <div class="accessories">
        {{#each viewModel.accessories}}
        <div class="accessory">
          <img src="{{this.imageUrl}}" alt="stickerName">
          <h3>{{this.name}}</h3>
          <p>{{this.description}}</p>
        </div>
        {{/each}}
      </div>
      {{else}}
      <h3 class="italic">This cube has no accessories yet...</h3>
      {{/if}}
    </main>
  </div>
  <footer>@Cubicle: Exercise for Express.js and Handlebars</footer>
</body>

</html>